prospect_for_resources = {
	prospectdecision_oil1 = {
        icon = oil
        allowed = {}
        available = {
			has_tech = excavation3
			num_of_civilian_factories_available_for_projects > 2
        }
    
        visible = {yes}

        state_target = yes
        target_array = ROOT.core_states
        on_map_mode = map_only
        
		target_trigger = {
            FROM = {
                has_state_flag = prospect_target_oil1
            }
        }

        cost = 25
		days_remove = 60
        modifier = {
			civilian_factory_use = 3
		}

		fire_only_once = no

        complete_effect = {
        }
    
        ai_will_do = { base = 0.3 modifier = { factor = 0.5 has_political_power < 80} modifier = { factor = 3 has_political_power > 200}
        }
        
        remove_effect = {
            FROM = { add_resource= {type = oil amount = 30}}
        }
    }
}