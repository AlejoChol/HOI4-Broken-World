add_namespace = inc_res

country_event = {
	id = inc_res.1
	
	title = inc_res.1.t
	desc = inc_res.1.d
	picture = GFX_report_event_resources
	
	fire_only_once = yes
	
	#hidden = no

	is_triggered_only = yes
		
	# ------------------------------
	# New resource generation system
	# ------------------------------
	
	# standard increase
	# ------------------------------
	# oil: 30 states 300 max
	# aluminum: 20 states 80 max
	# rubber: 10 states 400 max
	# tungsten: 20 states 200 max
	# steel: 50 states 100 max
	# chromium: 20 states 100 max
	# coal: 50 states 60 max
	
	# higher increase
	# ------------------------------
	# oil: 35 states 400 max
	# aluminum: 25 states 100 max
	# rubber: 15 states 500 max
	# tungsten: 25 states 250 max
	# steel: 65 states 150 max
	# chromium: 25 states 150 max
	# coal: 70 states 80 max
	
	# lower increase
	# ------------------------------
	# oil: 25 states 200 max
	# aluminum: 15 states 60 max
	# rubber: 8 states 300 max
	# tungsten: 15 states 150 max
	# steel: 40 states 80 max
	# chromium: 15 states 80 max
	# coal: 30 states 40 max

	# standard increase
	option = {
		name = inc_res.1.a
		custom_effect_tooltip = inc_res.1.a.d
		set_global_flag = resource_event_happened
		
		set_temp_variable = { temp = 0 }
		# oil: 30 states 300 max
		for_loop_effect = {
			end = 30
			value = temp
			random_state = {
				random_list={
					1={add_resource={type = oil amount =300}}
					2={add_resource={type = oil amount =200}}
					8={add_resource={type = oil amount = 80}}
					16={add_resource={type = oil amount = 20}}
				}
			}
		}
		
		# aluminum: 20 states 80 max
		for_loop_effect = {
			end = 20
			value = temp
			random_state = {
				random_list={
					1={add_resource={type = aluminium amount =80}}
					4={add_resource={type = aluminium amount = 40}}
					8={add_resource={type = aluminium amount = 20}}
				}
			}
		}
		
		# rubber: 10 states 400 max
		for_loop_effect = {
			end = 10
			value = temp
			random_state = {
				random_list={
					1={add_resource={type = rubber amount = 400}}
					2={add_resource={type = rubber amount = 300}}
					2={add_resource={type = rubber amount = 40}}
					1={add_resource={type = rubber amount = 20}}
				}
			}
		}

		# tungsten: 20 states 200 max
		for_loop_effect = {
			end = 20
			value = temp
			random_state = {
				random_list={
					1={add_resource={type = tungsten amount = 200}}
					4={add_resource={type = tungsten amount = 80}}
					8={add_resource={type = tungsten amount = 20}}
				}
			}
		}

		# steel: 50 states 100 max
		for_loop_effect = {
			end = 50
			value = temp
			random_state = {
				random_list={
					1={add_resource={type = aluminium amount = 100}}
					4={add_resource={type = aluminium amount = 60}}
					6={add_resource={type = aluminium amount = 40}}
				}
			}
		}

		# chromium: 20 states 100 max
		for_loop_effect = {
			end = 20
			value = temp
			random_state = {
				random_list={
					1={add_resource={type = aluminium amount = 100}}
					4={add_resource={type = aluminium amount = 80}}
					8={add_resource={type = aluminium amount = 20}}
				}
			}
		}
		# coal: 50 states 60 max
		for_loop_effect = {
			end = 50
			value = temp
			random_state = {
				random_list={
					1={add_resource={type = aluminium amount = 60}}
					4={add_resource={type = aluminium amount = 30}}
					8={add_resource={type = aluminium amount = 10}}
				}
			}
		}
	}
	
	# higher increase
	option = {
		name = inc_res.1.b
		custom_effect_tooltip = inc_res.1.b.d
		set_global_flag = resource_event_happened
		
		set_temp_variable = { temp = 0 }
		# oil: 35 states 400 max
		for_loop_effect = {
			end = 35
			value = temp
			random_state = {
				random_list={
					1={add_resource={type = oil amount =400}}
					2={add_resource={type = oil amount =300}}
					8={add_resource={type = oil amount = 120}}
					16={add_resource={type = oil amount = 30}}
				}
			}
		}
		
		# aluminum: 25 states 100 max
		for_loop_effect = {
			end = 25
			value = temp
			random_state = {
				random_list={
					1={add_resource={type = aluminium amount = 100}}
					4={add_resource={type = aluminium amount = 40}}
					8={add_resource={type = aluminium amount = 20}}
				}
			}
		}
		
		# rubber: 15 states 500 max
		for_loop_effect = {
			end = 15
			value = temp
			random_state = {
				random_list={
					1={add_resource={type = rubber amount = 500}}
					2={add_resource={type = rubber amount = 400}}
					2={add_resource={type = rubber amount = 100}}
					1={add_resource={type = rubber amount = 40}}
				}
			}
		}

		# tungsten: 25 states 250 max
		for_loop_effect = {
			end = 25
			value = temp
			random_state = {
				random_list={
					1={add_resource={type = tungsten amount = 250}}
					4={add_resource={type = tungsten amount = 100}}
					8={add_resource={type = tungsten amount = 40}}
				}
			}
		}

		# steel: 65 states 150 max
		for_loop_effect = {
			end = 65
			value = temp
			random_state = {
				random_list={
					1={add_resource={type = aluminium amount = 150}}
					4={add_resource={type = aluminium amount = 100}}
					6={add_resource={type = aluminium amount = 55}}
				}
			}
		}

		# chromium: 25 states 150 max
		for_loop_effect = {
			end = 25
			value = temp
			random_state = {
				random_list={
					1={add_resource={type = aluminium amount = 150}}
					4={add_resource={type = aluminium amount = 120}}
					8={add_resource={type = aluminium amount = 45}}
				}
			}
		}
		# coal: 70 states 80 max
		for_loop_effect = {
			end = 70
			value = temp
			random_state = {
				random_list={
					1={add_resource={type = aluminium amount = 80}}
					4={add_resource={type = aluminium amount = 50}}
					8={add_resource={type = aluminium amount = 25}}
				}
			}
		}
	}

	# lower increase
	option = {
		name = inc_res.1.c
		custom_effect_tooltip = inc_res.1.c.d
		set_global_flag = resource_event_happened
		
		set_temp_variable = { temp = 0 }
		# oil: 25 states 200 max
		for_loop_effect = {
			end = 25
			value = temp
			random_state = {
				random_list={
					1 ={add_resource={type = oil amount = 200}}
					2 ={add_resource={type = oil amount = 100}}
					8 ={add_resource={type = oil amount = 60}}
					16={add_resource={type = oil amount = 10}}
				}
			}
		}
		
		# aluminum: 15 states 60 max
		for_loop_effect = {
			end = 15
			value = temp
			random_state = {
				random_list={
					1={add_resource={type = aluminium amount = 60}}
					4={add_resource={type = aluminium amount = 30}}
					8={add_resource={type = aluminium amount = 10}}
				}
			}
		}
		
		# rubber: 8 states 300 max
		for_loop_effect = {
			end = 8
			value = temp
			random_state = {
				random_list={
					1={add_resource={type = rubber amount = 300}}
					2={add_resource={type = rubber amount = 200}}
					2={add_resource={type = rubber amount = 30}}
					1={add_resource={type = rubber amount = 10}}
				}
			}
		}

		# tungsten: 15 states 150 max
		for_loop_effect = {
			end = 15
			value = temp
			random_state = {
				random_list={
					1={add_resource={type = tungsten amount = 150}}
					4={add_resource={type = tungsten amount = 60}}
					8={add_resource={type = tungsten amount = 15}}
				}
			}
		}

		# steel: 40 states 80 max
		for_loop_effect = {
			end = 40
			value = temp
			random_state = {
				random_list={
					1={add_resource={type = aluminium amount = 80}}
					4={add_resource={type = aluminium amount = 40}}
					6={add_resource={type = aluminium amount = 20}}
				}
			}
		}

		# chromium: 15 states 80 max
		for_loop_effect = {
			end = 15
			value = temp
			random_state = {
				random_list={
					1={add_resource={type = aluminium amount = 80}}
					4={add_resource={type = aluminium amount = 65}}
					8={add_resource={type = aluminium amount = 10}}
				}
			}
		}
		
		# coal: 30 states 40 max
		for_loop_effect = {
			end = 30
			value = temp
			random_state = {
				random_list={
					1={add_resource={type = aluminium amount = 40}}
					4={add_resource={type = aluminium amount = 20}}
					8={add_resource={type = aluminium amount = 8}}
				}
			}
		}
	}
}